1. BOTH columns_gui.py and columns_logic.py MUST be in the same directory.

2. ALL image files:
	G1.png
	G2.png
	G3.png
	G4.png
	G5.png
	G6.png
	G7.png
	GL.png
	GM.png
	VP.jpg
	gameover.png
	---------------
	11 TOTAL Images

	MUST be in the same directory as BOTH columns_gui.py and columns_logic.py

3. columns_gui.py is the MAIN executable file with the (if __name == __'main'__:) block

4. DO NOT spam (Repeatedly press in short succession) input keys (Space, Right/Left Arrow),
	
	ESPECIALLY if all 3 jewels of a faller has not appeared yet.
